/**
 * 中国象棋规则引擎入口（占位）。
 *
 * 本模块为纯 TypeScript、零依赖实现：
 * - 不依赖 DOM、不依赖 Next.js
 * - 可同时被 src/components/ 前端组件与 functions/ 后端函数引用
 *
 * 计划导出：
 * - 类型：Piece / Board / Move / GameState
 * - 函数：generateMoves / isLegalMove / applyMove / isInCheck / getGameStatus
 * - 序列化：FEN 风格局面串与中文纵线记谱（如「炮二平五」）
 */
export {};
